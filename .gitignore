*pyc
.idea/
.idea/*
aviary.egg-info
build
dist
.snakemake
binsnek.egg-info
aviary_genome.egg-info

example/
test/data/.conda
test/data/wgsim.metaspades.assembly.fna.fai
*-log.txt
config.yaml
benchmarks
data
logs

# pixi environments
.pixi
*.egg-info
db
mqsub.out
test/qsub-test-log.txt
test_semibin_multi/*
test_semibin_multi.sh

# mkdocs build output — CI (.github/workflows/deploy-docs.yaml) builds this
# fresh from docs/ on every push and deploys it directly; it is never
# committed back, so a checked-in copy is stale build output, not the
# deployed site.
/site/
